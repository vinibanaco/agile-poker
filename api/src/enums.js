'use strict';

module.exports = Object.seal({
  CONFIG_KEYS: {
    ENV: 'env',
    PORT: 'port',
  },
  ENVS: {
    DEVELOPMENT: 'development',
    PRODUCTION: 'production',
    TEST: 'test',
  },
});
